import React from 'react';
import { FillIcon, ErrorIcon } from '..//icon/Icon';
import Alert from '.';

function demo() {
  return (
    <Alert type="default" closable description="不要熬夜哦亲亲" title="Don stay up late"></Alert>
  );
}

export default demo;
